me() {
   rcfile=$HOME/.sadhasivamrc
   if [ -f $rcfile ]; then
      echo "activated $rcfile file"
      source $rcfile
   fi
}

them() {
  rcfile=$HOME/.zshrc
  if [ -f $rcfile ]; then
      echo "activated $rcfile file"
      source $rcfile
  fi
}
